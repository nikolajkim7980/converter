import React, { useState } from "react";
import ReactDOM from "react-dom";

import "./styles.css";
import Front from "./front/Front";

const rootElement = document.getElementById("root");
ReactDOM.render(<Front />, rootElement);
